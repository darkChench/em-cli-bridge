{
  "_comment": "ZCode / 通用 MCP 配置示例（也适用于 VS Code、其他 stdio MCP 客户端）。放到工作区 .mcp.json 或用户配置。把下方 <repo> 和 <COMx> 替换为你本机的实际值。",
  "_placeholders": {
    "<repo>": "本仓库根目录的绝对路径",
    "<COMx>": "设备串口号，如 COM59",
    "<python>": "python 解释器；用 .venv 时改成 <repo>\\.venv\\Scripts\\python.exe"
  },
  "_config_path": "工作区 .mcp.json  或  用户 settings.json",
  "mcpServers": {
    "em-cli-bridge": {
      "command": "<python>",
      "args": [
        "<repo>\\mcp_server.py",
        "--port", "<COMx>"
      ]
    }
  }
}
